# 実機ごとのローカルメモ

USBポート、カメラ番号、キャリブレーション値はPCと個体ごとに異なります。このファイルには実値を記録せず、公開しないローカルメモへ保存してください。

## 確認項目

- LeRobotのバージョン
- フォロワーとリーダーのUSBポート
- 各カメラのindexまたはデバイスパス
- キャリブレーションJSONの保存場所
- 動作確認日と異常の有無

macOSでは `/dev/cu.*`、Linuxでは `/dev/ttyACM*` が一般的です。ポートは抜き差し前後の一覧を比較して特定します。
